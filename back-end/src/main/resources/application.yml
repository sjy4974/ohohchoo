spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ohohchoo?serverTimeZone=UTC
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        use-new-id-generator-mappings: false;
      dialect:
        org.hibernate.dialect.MySQL5InnoDBDialect

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

kakao:
  login:
    api_key: b675d62e80758a75de99af76108f06f5
    redirect_uri: http://localhost:8080/userApi/kakaoLogin

jwt:
  salt: ohohchoo
  validTime : 7200000

